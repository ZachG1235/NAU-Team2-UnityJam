using UnityEngine;
using UnityEngine.Pool;

public class VinStuff : MonoBehaviour
{
    public bool Vig = false;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Update() {
        if (Vig) { 
        }
       
    }
}
